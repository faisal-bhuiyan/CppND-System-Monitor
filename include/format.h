#ifndef FORMAT_H
#define FORMAT_H

#include <string>

namespace Format {
// Converts a provided time in seconds into HH:MM:SS format
std::string ElapsedTime(long times);
}; // namespace Format

#endif

